const initialState = {
    pictures: null
}

const galleryReducer = (state = initialState, action: any) => {
    switch (action.type) {
        default:
            return {
                ...state
            }


    }
}

export default galleryReducer